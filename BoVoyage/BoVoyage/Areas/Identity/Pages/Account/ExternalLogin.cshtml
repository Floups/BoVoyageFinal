@page
@model ExternalLoginModel
@{
    ViewData["Title"] = "Inscription";
}

<h1>@ViewData["Title"]</h1>
<h4>Associez votre compte @Model.LoginProvider.</h4>
<hr />

<p class="text-info">
    Vous vous êtes authentifié avec succès avec <strong>@Model.LoginProvider</strong>.
    Veuillez entrer une adresse mail pour le site ci-dessous et cliquer sur le bouton Enregistrer pour vous enregistrer.
</p>

<div class="row">
    <div class="col-md-4">
        <form asp-page-handler="Confirmation" asp-route-returnUrl="@Model.ReturnUrl" method="post">
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Input.Email"></label>
                <input asp-for="Input.Email" class="form-control" />
                <span asp-validation-for="Input.Email" class="text-danger"></span>
            </div>
            <button type="submit" class="btn btn-primary">Enregistrer</button>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
